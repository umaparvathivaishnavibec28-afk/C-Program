class Solution:
    def majorityElement(self, nums: list[int]) -> int:
        count = 0
        candidate = None

        for num in nums:
            if count == 0:
                candidate = num
            
            # If current number matches candidate, add 1, else subtract 1
            count += (1 if num == candidate else -1)

        return candidate
